export const DEFAULT_CONVERSATION_TITLE = "New Chat";

// Composer textarea 自动高度的行数边界，被 chat-shell 的自适应 effect 与
// composer 子组件的 min/max-height 内联样式共享。
export const MIN_TEXTAREA_ROWS = 1;
export const MAX_TEXTAREA_ROWS = 6;
